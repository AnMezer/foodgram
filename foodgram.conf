server {
    listen 80;
    server_name mezer.publicvm.com;
    client_max_body_size 5M;

    location / {
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $http_host;
        proxy_pass http://127.0.0.1:8000;
    }
}